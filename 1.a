
#include<stdio.h>
int main(){
  int n,i,sum;
  void perfect(int n);
  return 0;
  
 
  return 0;
}


void perfect(int n)
{

 
  for(n=1;n<=1000;n++){
    i=1;
    sum = 0;

    while(i<n){
      if(n%i==0)
           sum=sum+i;
          i++;
    }

    if(sum==n)
      printf("%d ",n);
  }

